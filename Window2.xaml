<Window x:Class="Animals.Window2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Animals"
        mc:Ignorable="d"
        Title="Window2" Height="450" Width="800">
    <Grid x:Name="ctlGrid">
        <Grid.ColumnDefinitions>

            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />

            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBox Grid.Column="0" Grid.Row="0" Name="TBname" Text="{Binding name}" TextAlignment="Center"/>

        <TextBox Grid.Column="1" Grid.Row="0" Name="TBspecies" Text="{Binding species}" TextAlignment="Center"/>

        <TextBox Grid.Column="2" Grid.Row="0" Name="TBage"  Text="{Binding age}" TextAlignment="Center"/>

        <Button Grid.ColumnSpan="3" Grid.Row="1"  FontSize="40" Content="Ok" />

        <!-- Click="Button_Aktualizuj" <Button Grid.Column="2" Grid.Row="1"  FontSize="40" Content="Zmien" Click="Button_Zmien"/> -->

    </Grid>
</Window>
